"use client";

import React from "react";
import { SignupPage } from "../../sections/signup/SignupPage/SignupPage";

export default function Page() {
  return (
    <SignupPage
    // email={email}
    // href={href}
    // name={name}
    // password={password}
    // onClick={onClick}
    />
  );
}
